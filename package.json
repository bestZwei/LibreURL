{
  "name": "short-url-project",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build step not required'",
    "deploy": "npx wrangler pages publish public --project-name short-url-project"
  }
}